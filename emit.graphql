# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

"""
The javascript `Date` as string. Type represents date and time as the ISO Date string.
"""
scalar DateTime

type DResult {
  affected: Float!
}

type Mutation {
  addRecipe(recipe: RecipeInput!): Recipe!
  rate(rate: RateInput!): Recipe!
  addPost(post: PostInput!): Post!
  deletePost(postId: Int!): DResult!
  updatePost(post: PostInput!): UResult
}

type Post {
  id: ID!
  title: String!
  comment: String!
}

input PostInput {
  id: String
  title: String!
  comment: String!
  completed: Boolean
}

type Query {
  recipe(recipeId: Int!): Recipe
  recipes: [Recipe!]!
  posts: [Post!]!
  post(postId: Int!): Post
}

type Rate {
  value: Int!
  user: User!
  date: DateTime!
}

input RateInput {
  recipeId: ID!
  value: Int!
}

type Recipe {
  id: ID!
  title: String!
  description: String
  ratings: [Rate!]!
  author: User!
}

input RecipeInput {
  title: String!
  description: String
}

type UResult {
  affected: Float!
}

type User {
  id: ID!
  email: String!
  nickname: String
  recipes: [Recipe!]!
}
